<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 네임스페이스를 DAO에서 호출하는 경로와 일치시킵니다. -->
<mapper namespace="mapper.reservation">

	<!-- =================== Result Maps =================== -->
	<resultMap id="pensionDetailResult" type="com.petmillie.business.vo.PensionVO">
		<result property="p_num" column="p_num" />
		<result property="business_id" column="business_id" />
		<result property="p_name" column="p_name" />
		<result property="tel1" column="tel1" />
		<result property="tel2" column="tel2" />
		<result property="tel3" column="tel3" />
		<result property="checkin_time" column="checkin_time" />
		<result property="checkout_time" column="checkout_time" />
		<result property="facilities" column="facilities" />
		<result property="description" column="description" />
		
		<!-- [최종 수정] DB의 fileName 컬럼과 PensionVO의 fileName 필드를 연결합니다. -->
		<result property="fileName" column="fileName" />
		
		<!-- business 테이블과 JOIN한 결과를 매핑 -->
		<association property="business" javaType="com.petmillie.business.vo.BusinessVO">
			<result property="roadAddress" column="roadAddress"/>
			<result property="jibunAddress" column="jibunAddress"/>
		</association>
	</resultMap>

	<resultMap id="roomResult" type="com.petmillie.business.vo.RoomVO">
		<result property="room_id" column="room_id"/>
		<result property="p_num" column="p_num"/>
		<result property="room_name" column="room_name"/>
		<result property="room_type" column="room_type"/>
		<result property="price" column="price"/>
		<result property="max_capacity" column="max_capacity"/>
		<result property="bed_type" column="bed_type"/>
		<result property="room_size" column="room_size"/>
		<result property="room_description" column="room_description"/>
		<result property="amenities" column="amenities"/>
		<result property="reg_date" column="reg_date"/>
		<result property="del_yn" column="del_yn"/>
		<result property="fileimage" column="fileimage"/>
	</resultMap>
	
	<resultMap id="reservationResult" type="com.petmillie.reservation.vo.ReservationVO">
		<result property="reservation_id" column="reservation_id"/>
		<result property="member_id" column="member_id"/>
		<result property="business_id" column="business_id"/>
		<result property="room_id" column="room_id"/>
		<result property="checkin_date" column="checkin_date"/>
		<result property="checkout_date" column="checkout_date"/>
		<result property="guests" column="guests"/>
		<result property="reserver_name" column="reserver_name"/>
		<result property="reserver_tel" column="reserver_tel"/>
		<result property="total_price" column="total_price"/>
		<result property="reservation_status" column="reservation_status"/>
		
		<!-- JOIN 결과를 매핑하기 위한 필드들 -->
		<result property="p_name" column="p_name"/>
		<result property="roadAddress" column="roadAddress"/>
		<result property="room_name" column="room_name"/>
		<result property="room_type" column="room_type"/>
		<result property="bed_type" column="bed_type"/>
	</resultMap>

	<!-- =================== SQL Queries =================== -->

	<!-- 펜션 목록 전체 조회 (JOIN과 resultMap 사용) -->
	<select id="selectAllPensionList" resultMap="pensionDetailResult">
    <![CDATA[
        SELECT p.*, b.roadAddress, b.jibunAddress
        FROM pension p
        JOIN business b ON p.business_id = b.business_id
        WHERE p.pension_status = 'approve'
        ORDER BY p.p_num DESC
    ]]>
	</select>
	
	<!-- 펜션 상세 정보 조회 -->
	<select id="selectPensionDetail" resultMap="pensionDetailResult" parameterType="int">
		<![CDATA[
			SELECT
				p.*, b.roadAddress, b.jibunAddress
			FROM
				pension p
			JOIN
				business b ON p.business_id = b.business_id
			WHERE
				p.p_num = #{p_num}
		]]>
	</select>

	<!-- 펜션의 객실 목록 조회 -->
	<select id="selectRoomList" resultMap="roomResult" parameterType="int">
    <![CDATA[
        SELECT * FROM room 
        WHERE p_num = #{p_num} AND room_status = '예약가능' 
        ORDER BY price ASC
    ]]>
	</select>

	<!-- 특정 객실 상세 정보 조회 -->
	<select id="selectRoomDetail" resultMap="roomResult" parameterType="int">
		<![CDATA[
			SELECT * FROM room
			WHERE room_id = #{roomId}
		]]>
	</select>
	
	<!-- 예약 정보 추가 -->
	<insert id="insertReservation" parameterType="com.petmillie.reservation.vo.ReservationVO">
	  INSERT INTO reservation(
	      p_num,
	      member_id,
	      business_id,
	      room_id,
	      checkin_date,
	      checkout_date,
	      guests,
	      reserver_name,
	      reserver_tel,
	      total_price,
	      reservation_status
	  ) VALUES (
	      #{p_num},
	      #{member_id},
	      #{business_id},
	      #{room_id},
	      #{checkin_date},
	      #{checkout_date},
	      #{guests},
	      #{reserver_name},
	      #{reserver_tel},
	      #{total_price},
	      #{reservation_status}
	  )
	</insert>
	
	<!-- 사업자 ID로 예약 목록 조회 -->
	<select id="selectReservationsByBusinessId" resultMap="reservationResult" parameterType="String">
		<![CDATA[
			SELECT 
				r.*, 
				p.p_name,
				b.roadAddress,
				rm.room_name, 
				rm.room_type,
				rm.bed_type
			FROM reservation r
			JOIN pension p ON r.business_id = p.business_id
			JOIN room rm ON r.room_id = rm.room_id
			JOIN business b ON p.business_id = b.business_id
			WHERE r.business_id = #{business_id}
			ORDER BY r.checkin_date DESC
		]]>
	</select>

	<!-- 회원 ID로 예약 목록 조회 -->
	<select id="selectReservationsByMemberId" resultMap="reservationResult" parameterType="String">
		<![CDATA[
			SELECT 
				r.*, 
				p.p_name,
				b.roadAddress,
				rm.room_name, 
				rm.room_type,
				rm.bed_type
			FROM reservation r
			JOIN pension p ON r.business_id = p.business_id
			JOIN room rm ON r.room_id = rm.room_id
			JOIN business b ON p.business_id = b.business_id
			WHERE r.member_id = #{memberId}
			ORDER BY r.checkin_date DESC
		]]>
	</select>
	
	<!-- 예약 ID로 특정 예약 정보 조회 -->
	<select id="selectReservationById" resultMap="reservationResult" parameterType="int">
    <![CDATA[
        SELECT 
            r.*, 
            p.p_name,
            rm.room_name
        FROM reservation r
        JOIN pension p ON r.p_num = p.p_num
        JOIN room rm ON r.room_id = rm.room_id
        WHERE r.reservation_id = #{reservation_id}
    ]]>
	</select>
	
	<!-- 예약 정보 수정 -->
	<update id="updateReservation" parameterType="com.petmillie.reservation.vo.ReservationVO">
    <![CDATA[
        UPDATE reservation
        SET 
            reserver_name = #{reserver_name},
            reserver_tel = #{reserver_tel},
            checkin_date = #{checkin_date},
            checkout_date = #{checkout_date},
            guests = #{guests},
            total_price = #{total_price}
        WHERE 
            reservation_id = #{reservation_id}
    ]]>
	</update>
	
	<!-- 예약 상태 변경 -->
	<update id="updateReservationStatus" parameterType="map">
	    UPDATE reservation
	    SET reservation_status = #{status}
	    WHERE reservation_id = #{reservationId}
	</update>
	
	<!-- 예약 취소로 상태 변경 -->
	<update id="updateReservationStatusToCancel" parameterType="int">
        UPDATE reservation
        SET reservation_status = '예약취소'
        WHERE reservation_id = #{reservation_id}
  </update>
  
  <!-- 객실 가격 조회 -->
  <select id="selectRoomPrice" parameterType="int" resultType="int">
      SELECT price 
      FROM room 
      WHERE room_id = #{roomId}
  </select>
  
  <!-- 관리자가 모든 예약 목록을 조회 -->
	<select id="selectAllReservations" resultMap="reservationResult">
	    <![CDATA[
	        SELECT 
	            r.*, 
	            p.p_name,
	            rm.room_name
	        FROM reservation r
	        LEFT JOIN pension p ON r.p_num = p.p_num
	        LEFT JOIN room rm ON r.room_id = rm.room_id
	        ORDER BY r.reservation_id DESC
	    ]]>
	</select>
    
</mapper>